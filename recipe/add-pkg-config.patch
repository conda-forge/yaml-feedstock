diff --git a/CMakeLists.txt b/CMakeLists.txt
index 4f81148..b2ac055 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -65,6 +65,8 @@ set_target_properties(yaml
   PROPERTIES DEFINE_SYMBOL YAML_DECLARE_EXPORT
   )
 
+set_target_properties(yaml PROPERTIES SOVERSION ${YAML_VERSION_MAJOR}.${YAML_VERSION_MINOR})
+
 target_compile_definitions(yaml
   PRIVATE HAVE_CONFIG_H
   PUBLIC
@@ -158,3 +160,16 @@ install(
   FILES ${config_version_file}
   DESTINATION ${INSTALL_CMAKE_DIR} COMPONENT Development
   )
+
+# Generate pkg-config files
+set(PC_CONFIG_IN ${CMAKE_CURRENT_SOURCE_DIR}/cmake/yaml-0.1.pc.in)
+set(PC_CONFIG_OUT ${CMAKE_CURRENT_BINARY_DIR}/cmake/yaml-0.1.pc)
+set(PC_CONFIG_INSTALL_DIR ${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_LIBDIR}/pkgconfig)
+file(RELATIVE_PATH
+  RELATIVE_PATH_TO_INSTALL_PREFIX
+  "${PC_CONFIG_INSTALL_DIR}"
+  "${CMAKE_INSTALL_PREFIX}"
+)
+
+configure_file(${PC_CONFIG_IN} ${PC_CONFIG_OUT} @ONLY)
+install(FILES ${PC_CONFIG_OUT} DESTINATION ${PC_CONFIG_INSTALL_DIR})
diff --git a/cmake/yaml-0.1.pc.in b/cmake/yaml-0.1.pc.in
new file mode 100644
index 0000000..bc7ca21
--- /dev/null
+++ b/cmake/yaml-0.1.pc.in
@@ -0,0 +1,11 @@
+# This file was generated by CMake for @PROJECT_NAME@
+prefix=@CMAKE_INSTALL_PREFIX@
+exec_prefix=${prefix}
+includedir=${prefix}/@INSTALL_INCLUDE_DIR@
+libdir=${exec_prefix}/@CMAKE_INSTALL_LIBDIR@
+
+Name: LibYAML
+Description: Library to parse and emit YAML
+Version: @YAML_VERSION_STRING@
+Cflags: -I${includedir}
+Libs: -L${libdir} -lyaml
